# frontend/.env.development
# React environment variables must start with REACT_APP_
REACT_APP_API_URL=http://localhost:8000/api/

# Optional: Set browser to none to prevent opening automatically inside container
# BROWSER=none

# Ensures Create React App's fast refresh works correctly with Docker polling
CHOKIDAR_USEPOLLING=true
